/*functon myMatches () {
    let email = localStorage.getItem("email");



fetch(`http://localhost:5000/users/${email}`{
    method:'GET',
    redirect:'manual'
)}

  .then((resp) => resp.json())
  .then(function (match) {
      console.log(match);
    }
});*/

/*functions 
1. get req for the people in the database
2. Post-req where you will create a new match
3. no -> reload another page and show another person*/